# إنشاء التصنيف تلقائياً عند إنشاء الصفحة

## الميزة الجديدة
عند إنشاء صفحة جديدة، أصبح اختيار التصنيف (Category) اختيارياً.
إذا ترك المستخدم حقل التصنيف فارغاً، سيقوم النظام تلقائياً بإنشاء تصنيف جديد يحمل نفس اسم الصفحة ورابطها (Slug)، ثم يربط الصفحة بهذا التصنيف الجديد.

## التعديلات التقنية

### 1. `PageForm.jsx`
- تم إزالة شرط `required` من حقل `categoryId`.
- أصبح بإمكان المستخدم تجاوز اختيار التصنيف.

### 2. `PageBuilderPage.jsx`
- في دالة `handleSave` (عند إنشاء صفحة جديدة):
  - يتم التحقق مما إذا كان `pageData.categoryId` فارغاً.
  - إذا كان فارغاً:
    1. يتم استدعاء `categoriesApi.createCategory` لإنشاء تصنيف جديد.
    2. يتم استخدام بيانات الصفحة (`nameEn`, `nameAr`, `nameKu`, `slug`) لبيانات التصنيف.
    3. يتم استخراج `id` التصنيف الجديد من استجابة الـ API.
    4. يتم استخدام هذا الـ `id` لإنشاء الصفحة (`createPageWithComponents`).

## سيناريو الاستخدام
1. يذهب المستخدم لإنشاء صفحة جديدة.
2. يدخل اسم الصفحة "About Us".
3. يترك حقل "Category" فارغاً.
4. يضغط "Next".
5. يبني محتوى الصفحة ويضغط "Save".
6. النظام يقوم بـ:
   - إنشاء تصنيف جديد اسمه "About Us".
   - إنشاء الصفحة "About Us" وربطها بالتصنيف الجديد.

## ملاحظات
- إذا فشل إنشاء التصنيف التلقائي (مثلاً بسبب تكرار الاسم أو Slug)، ستظهر رسالة خطأ للمستخدم تطلب منه اختيار تصنيف يدوياً.
